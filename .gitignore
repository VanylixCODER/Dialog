node_modules/

# Приватный ключ TLS и сертификат — НИКОГДА не публиковать
certs/

# Данные пользователей (хеши паролей) — не в публичный репозиторий
data/

# Local helper scripts — not part of the shipped app
scripts/

# Логи и временное
*.log
.DS_Store
.env
Dialog.apk
ssh-key-*.key
ssh-key-*.key.pub
# Electron build artifacts
build/
dist/
build.sh
# Session logs
session-*.md
# VNC / local junk
*.vnc
desktop/dialog-desktop-1.0.1-linux-x64.tar.gz

# Firebase / FCM secrets — NEVER commit to the public repo
docs/google-services.json
android/app/google-services.json
**/*firebase-adminsdk*.json
**/*service-account*.json
docs/*service*.json

# Play upload signing key — NEVER commit (back this up!)
*.jks
*.keystore
android/upload-keystore.jks
docs/upload-key-credentials.txt

# Build artifacts (Electron / Flatpak / packaged installers)
dist/
.flatpak-builder/
*.AppImage
*.exe
*.dmg
*.deb
*.pacman

# Dev-only material that should never be committed. dev/ IS tracked (design references
# live there on purpose) — this is for scratch space inside it.
dev/scratch/
